# Procfile for deployment on platforms like Heroku, Railway, Render, etc.

# Production deployment - runs the Node.js server
web: npm start

# Alternative for static-only deployment (if you prefer to serve frontend separately)
# web: npx serve -s frontend/dist -l $PORT

# Development (for local development with both frontend and backend)
# dev: npm run dev
